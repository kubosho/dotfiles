# ------------------------------
# Common Environments
# ------------------------------
[ -f "$HOME/.commonenv" ] && . "$HOME/.commonenv"

# ------------------------------
# Zsh Configuration
# ------------------------------
export ZSH_CONFIG_DIR="${XDG_CONFIG_HOME}/zsh"
